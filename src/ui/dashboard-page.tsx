import type { Workout } from '../db/entities';
type Props = { workouts: Workout[]; onStartWorkout: () => void; onNavigate: (section: 'history' | 'exercises' | 'templates') => void };
const formatDate = (date: string) => new Intl.DateTimeFormat('ru-RU', { day: 'numeric', month: 'short' }).format(new Date(date));
export function DashboardPage({ workouts, onStartWorkout, onNavigate }: Props) {
  return <section className="content-page dashboard-page" aria-labelledby="dashboard-title"><header className="page-header dashboard-header"><div><p className="eyebrow">Журнал тренировок</p><h1 id="dashboard-title">Обзор</h1><p className="muted">Готов к следующему подходу?</p></div></header><button className="hero-start" type="button" aria-label="Начать тренировку" onClick={onStartWorkout}><span className="hero-icon">＋</span><span><strong>Начать тренировку</strong><small>Пустая тренировка или шаблон</small></span><span className="hero-arrow">→</span></button><div className="section-heading"><h2>Последние тренировки</h2><button className="text-button" type="button" onClick={() => onNavigate('history')}>Вся история</button></div><div className="recent-list">{workouts.slice(0, 3).map((workout) => <button className="recent-item" type="button" key={workout.id} onClick={() => onNavigate('history')}><span className="recent-mark">✓</span><span><strong>{workout.templateId ? 'Тренировка по шаблону' : 'Свободная тренировка'}</strong><small>{formatDate(workout.date)} · {workout.exercises.length} упражнений</small></span><span className="recent-chevron">›</span></button>)}{workouts.length === 0 && <div className="empty-state"><span>✦</span><strong>История пока пуста</strong><p>Начни первую тренировку — здесь появятся сохранённые занятия.</p></div>}</div></section>;
}
