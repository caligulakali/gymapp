import { useEffect, useState, type FormEvent } from 'react';
import type { Exercise, Template, Workout, WorkoutSet } from '../../db/entities';
import type { ExerciseRepositoryPort } from '../../domain/exercise-repository-port';
import type { TemplateRepositoryPort } from '../../domain/template-repository-port';
import type { WorkoutRepositoryPort } from '../../domain/workout-repository-port';

type Props = { workoutRepository: WorkoutRepositoryPort; templateRepository: TemplateRepositoryPort; exerciseRepository: ExerciseRepositoryPort; createId: () => string; now: () => string; onSaved?: () => void };
const emptySet = (): WorkoutSet => ({});
export function WorkoutPage({ workoutRepository, templateRepository, exerciseRepository, createId, now, onSaved }: Props) {
  const [templates, setTemplates] = useState<Template[]>([]); const [exercises, setExercises] = useState<Exercise[]>([]); const [active, setActive] = useState<Workout>(); const [notes, setNotes] = useState(''); const [saved, setSaved] = useState(false);
  useEffect(() => { void Promise.all([templateRepository.getAll(), exerciseRepository.getAll()]).then(([nextTemplates, nextExercises]) => { setTemplates(nextTemplates); setExercises(nextExercises); }); }, [templateRepository, exerciseRepository]);
  const start = (template?: Template) => { setActive({ id: createId(), templateId: template?.id, date: now(), exercises: template?.exercises.map((item) => ({ exerciseId: item.exerciseId, order: item.order, sets: Array.from({ length: item.sets }, () => ({ reps: item.targetReps, weight: item.targetWeight })) })) ?? [] }); setSaved(false); };
  const addExercise = (exerciseId: string) => { if (!active || active.exercises.some((item) => item.exerciseId === exerciseId)) return; setActive({ ...active, exercises: [...active.exercises, { exerciseId, order: active.exercises.length, sets: [emptySet()] }] }); };
  const updateSet = (exerciseIndex: number, setIndex: number, key: keyof WorkoutSet, value: string) => { if (!active) return; setActive({ ...active, exercises: active.exercises.map((exercise, index) => index !== exerciseIndex ? exercise : { ...exercise, sets: exercise.sets.map((set, current) => current !== setIndex ? set : { ...set, ...(value ? { [key]: Number(value) } : {}) }) }) }); };
  async function save(event: FormEvent) { event.preventDefault(); if (!active) return; await workoutRepository.save({ ...active, notes: notes || undefined }); setSaved(true); onSaved?.(); }
  if (!active) return <section className="content-page workout-picker" aria-labelledby="workout-picker-title"><header className="page-header"><div><p className="eyebrow">Тренировка</p><h1 id="workout-picker-title">Начать тренировку</h1><p className="muted">Выбери шаблон или начни с чистого листа</p></div></header><button className="hero-start" type="button" onClick={() => start()}><span className="hero-icon">＋</span><span><strong>Пустая тренировка</strong><small>Добавь упражнения по ходу</small></span><span className="hero-arrow">→</span></button><h2 className="picker-title">Твои шаблоны</h2><div className="template-picker">{templates.map((template) => <button key={template.id} type="button" aria-label={`Начать тренировку по шаблону ${template.name}`} onClick={() => start(template)}><span className="template-picker-icon">▦</span><span><strong>{template.name}</strong><small>{template.exercises.length} упражнений</small></span><span>›</span></button>)}{templates.length === 0 && <p className="muted">Создай шаблон в разделе «Шаблоны», чтобы запускать его в один клик.</p>}</div></section>;
  return <section className="content-page workout-page" aria-labelledby="active-workout-title"><header className="page-header"><div><p className="eyebrow">В процессе</p><h1 id="active-workout-title">Тренировка: {active.templateId ? templates.find((item) => item.id === active.templateId)?.name : 'Свободная'}</h1><p className="muted">{active.exercises.length} упражнений · {active.exercises.reduce((sum, item) => sum + item.sets.length, 0)} подходов</p></div><span className="timer-pill">● 00:00</span></header><form onSubmit={(event) => void save(event)}><div className="workout-exercises">{active.exercises.map((item, exerciseIndex) => { const exercise = exercises.find((candidate) => candidate.id === item.exerciseId); const name = exercise?.name ?? item.exerciseId; return <article className="workout-card" key={item.exerciseId}><div className="workout-card-heading"><div><span className="exercise-number">{String(exerciseIndex + 1).padStart(2, '0')}</span><h2>{name}</h2></div><span className="muted">{item.sets.length} подхода</span></div><div className="set-header"><span>Подход</span><span>Вес</span><span>Повторы</span><span>Дополнительно</span></div>{item.sets.map((set, setIndex) => <div className="set-row" key={setIndex}><span className="set-number">{setIndex + 1}</span><input aria-label={`Вес подхода ${setIndex + 1} для ${name}`} type="number" min="0" step="0.5" value={set.weight ?? ''} onChange={(event) => updateSet(exerciseIndex, setIndex, 'weight', event.target.value)} /><input aria-label={`Повторы подхода ${setIndex + 1} для ${name}`} type="number" min="0" value={set.reps ?? ''} onChange={(event) => updateSet(exerciseIndex, setIndex, 'reps', event.target.value)} /><input aria-label={`Время подхода ${setIndex + 1} для ${name}`} type="number" min="0" value={set.time ?? ''} onChange={(event) => updateSet(exerciseIndex, setIndex, 'time', event.target.value)} /><input aria-label={`Расстояние подхода ${setIndex + 1} для ${name}`} type="number" min="0" step="0.1" value={set.distance ?? ''} onChange={(event) => updateSet(exerciseIndex, setIndex, 'distance', event.target.value)} /><input aria-label={`Отдых после подхода ${setIndex + 1} для ${name}`} type="number" min="0" value={set.rest ?? ''} onChange={(event) => updateSet(exerciseIndex, setIndex, 'rest', event.target.value)} /><button className="set-check" type="button" aria-label={`Удалить подход ${setIndex + 1} для ${name}`} onClick={() => setActive({ ...active, exercises: active.exercises.map((current, index) => index !== exerciseIndex ? current : { ...current, sets: current.sets.filter((_, currentSet) => currentSet !== setIndex) }) })}>×</button></div>)}<button className="add-set" type="button" onClick={() => setActive({ ...active, exercises: active.exercises.map((current, index) => index !== exerciseIndex ? current : { ...current, sets: [...current.sets, emptySet()] }) })}>＋ Добавить подход</button></article>; })}</div><div className="add-exercise-row"><span>Добавить упражнение</span><div>{exercises.filter((exercise) => !active.exercises.some((item) => item.exerciseId === exercise.id)).map((exercise) => <button className="chip-button" type="button" key={exercise.id} aria-label={`Добавить ${exercise.name}`} onClick={() => addExercise(exercise.id)}>＋ {exercise.name}</button>)}</div></div><label className="notes-field">Заметки тренировки<textarea aria-label="Заметки тренировки" value={notes} onChange={(event) => setNotes(event.target.value)} /></label><button className="primary-submit" type="submit">Завершить и сохранить тренировку</button>{saved && <p className="saved-message">Тренировка сохранена</p>}</form></section>;
}
