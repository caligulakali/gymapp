import type { Workout } from '../db/entities';
export function ProgressPage({ workouts }: { workouts: Workout[] }) { const totalSets = workouts.reduce((sum, workout) => sum + workout.exercises.reduce((exerciseSum, exercise) => exerciseSum + exercise.sets.length, 0), 0); const days = new Set(workouts.map((workout) => new Date(workout.date).toLocaleDateString('ru-RU'))).size; return <section className="content-page" aria-labelledby="progress-title"><header className="page-header"><div><p className="eyebrow">Аналитика</p><h1 id="progress-title">Прогресс</h1><p className="muted">Твои результаты в цифрах</p></div></header><div className="progress-hero"><div><span className="muted">Всего тренировок</span><strong>{workouts.length}</strong></div><div className="progress-ring"><span>{days}</span><small>дней</small></div></div><div className="stat-grid"><article className="stat-card"><strong>{totalSets}</strong><small>Всего подходов</small></article><article className="stat-card"><strong>{days}</strong><small>Активных дней</small></article><article className="stat-card"><strong>{workouts.length ? 'Есть' : '—'}</strong><small>Данные для анализа</small></article></div><div className="chart-card"><div className="section-heading"><h2>Активность</h2><span className="muted">Последние 7 дней</span></div><div className="bars" aria-label="График активности">{[20, 42, 28, 66, 48, 84, workouts.length ? 58 : 8].map((height, index) => <span key={index} style={{ height: `${height}%` }} />)}</div><div className="chart-labels"><span>Пн</span><span>Вт</span><span>Ср</span><span>Чт</span><span>Пт</span><span>Сб</span><span>Вс</span></div></div></section>; }
