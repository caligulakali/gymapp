import type { Workout } from '../db/entities';
export function HistoryPage({ workouts }: { workouts: Workout[] }) { return <section className="content-page" aria-labelledby="history-title"><header className="page-header"><div><p className="eyebrow">Журнал</p><h1 id="history-title">История</h1><p className="muted">Все завершённые тренировки</p></div><button className="secondary-button" type="button">Фильтр</button></header><div className="history-list">{workouts.map((workout) => <article className="history-card" key={workout.id}><div className="history-date"><strong>{new Date(workout.date).getDate()}</strong><span>{new Date(workout.date).toLocaleDateString('ru-RU', { month: 'short' })}</span></div><div><h3>{workout.templateId ? 'Тренировка по шаблону' : 'Свободная тренировка'}</h3><p className="muted">{workout.exercises.length} упражнений · {workout.exercises.reduce((sum, item) => sum + item.sets.length, 0)} подходов</p></div><span className="recent-chevron">›</span></article>)}{workouts.length === 0 && <div className="empty-state"><span>◌</span><strong>Пока ничего нет</strong><p>Сохранённые тренировки будут отображаться здесь.</p></div>}</div></section>; }
